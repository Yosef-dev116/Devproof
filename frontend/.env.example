# The backend API's public URL. Leave unset for local dev (defaults to
# http://localhost:8002). Set this in Vercel's project environment variables
# to your deployed Render backend URL, e.g. https://devproof-api.onrender.com
VITE_API_BASE_URL=http://localhost:8002
